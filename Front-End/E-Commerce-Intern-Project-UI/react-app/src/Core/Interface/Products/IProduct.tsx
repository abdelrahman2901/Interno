export interface IProduct {
  productID: string;
  categoryID: string;
  productName: string;
  price: number;
  rating: number;
  productImageUrl: string;
  hashImage: string;

  createdAt: string;
}
